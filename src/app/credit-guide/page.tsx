import CreditGuideContent from './CreditGuideContent';

export const metadata = {
  title: 'PodcastAI Credit Calculation Guide',
  description: 'Learn exactly how PodcastAI credits are calculated, including base costs, promotion fees, voice options, and scheduling extras.',
};

export default function CreditGuidePage() {
  return <CreditGuideContent />;
} 